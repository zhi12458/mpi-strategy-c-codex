# 给新用户的一次性安装提示词

> 请从公开仓库 `https://github.com/zhi12458/mpi-strategy-c-codex` 安装并配置 `mpi-strategy-c` 技能，在同一个任务中完成依赖检查、锁定仓库、模型选择、凭据保存、doctor、故障恢复测试和无私人内容冒烟测试。不得把 MPI 与 toolkit 当作参考文件：每次翻译都必须读取两套规范，并通过技能审计器实际调用锁定版 toolkit 的提取、MPI 术语检索、Djot 管理、双语生成、机械检查、DOCX 和字幕质量工具。任一上游未正式发布、SHA/origin/哈希不符、doctor 失败或收据缺失时必须停止，不能降级。需要 DeepSeek 密钥时请打开系统隐藏输入窗口并保存到系统安全凭据库；不要让我把密钥贴进聊天。需要 Whisper 模型时打开文件选择窗口，只接受哈希匹配的 `whisper-medium-2512-ft-best-ggml.bin`。只有 `READY.json` 明确为 `ready: true` 才告诉我安装完成。此后我只需说“请翻译这个文件、音频或视频”。
