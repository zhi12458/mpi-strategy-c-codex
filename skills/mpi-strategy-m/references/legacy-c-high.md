# Archived original Strategy C baseline

The original Strategy C used GPT-5.6-Sol `high` for the full document and its
translator-owned revisions. It remains a reproducible historical comparison,
not an active installable skill. Do not route ordinary work or “M方案” requests
to it.

Preserve its source lock, model policy, benchmark artifacts, and comparison
scores. Never silently label a later M result as an original C result.
